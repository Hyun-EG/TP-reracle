import React from 'react';
import { Layout } from '../layout/Layout';

const Newspaper = () => (
    <Layout>
      <h1>신문지</h1>
      <div>배출방법</div>
      <p>
        쓰레기는 재활용하지 않으면 매립하거나 소각하여야 하는데, 이러면 돈이 든다. 때문에 버릴 때마다 돈을
        내고서 버리거나 정부나 지자체에서 예산을 들여서 처리해야 한다. 하지만 분리배출된 쓰레기는 그대로
        가져가서 재활용할 수 있기 때문에 재활용 업체에서 가져가 준다. 덕분에 대부분의 재활용 쓰레기는
        분리배출하면 공짜로 버릴 수 있다. 하지만 재활용에도 회수, 재분류, 재처리 등의 비용이 들고 재활용해도
        값이 잘 안 나오는 쓰레기도
      </p>
    </Layout>
  );

export default Newspaper;
